name: test
on:
  pull_request:

env:
  SIRIUS_DOCKER_HOST: ""

jobs:
  test:
    runs-on: ubuntu-latest
    container:
      image: scireum/sirius-build-jdk18:latest
    steps:
      - name: Checkout project
        uses: actions/checkout@v3

      # During workflow/job "test" on GitHub, tests are run only without framework-start because of sirius-kernel issues.
      # Locally, tests are running well since the framework starts without any issues.
      - name: Compile and test devops project
        run: mvn clean compile test -Dtest.excluded.groups=local
